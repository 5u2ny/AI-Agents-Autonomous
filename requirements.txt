# No API key needed. These agents drive whatever coding-agent CLI you already
# have logged in (Claude Code, Codex, Gemini, Copilot, Cursor, ...).
#
# The engine itself is pure Python standard library. The only (optional)
# dependency is python-dotenv, for loading a .env file.
python-dotenv>=1.0.0
